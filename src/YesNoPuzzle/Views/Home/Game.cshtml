@using Microsoft.EntityFrameworkCore.Metadata.Internal
@using YesNoPuzzle.Models
@{
    ViewData["Title"] = "Home Page";
}
@model YesNoPuzzle.Models.GameViewModels.GameViewModel

@{
    <h2>@Model.Game.GameName</h2>
    <h3>@Model.Game.GameCondition</h3>

    <hr/>

    if (Model.Questions != null && Model.Questions.Count > 0)
    {
        <h4>Questions:</h4>

        foreach (var q in @Model.Questions)
        {
            <p>@q.Text</p>
        }
    }
    else
    {
        <h4>No questions yet.</h4>
    }


    <form asp-controller="Home" asp-action="AddNewQuestion" method="post" class="form-horizontal">
        <div class="form-group">
            <input type="text" asp-for="@Model.Text"/><br/>
            <input type="hidden" asp-for="@Model.GameId"/>
        </div>
        <div class="form-group">
            <button class="btn btn-primary">Ask</button>
        </div>

    </form>
}
